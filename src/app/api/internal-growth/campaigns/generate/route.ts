import { NextResponse } from "next/server"; import { requireGrowthAgentOwnerContext } from "@/features/internal-growth/server/guards"; import { generateCampaign } from "@/features/internal-growth/server/repository"; import { toEndpointErrorResponse, ShopReelEndpointError } from "@/features/shopreel/server/endpointPolicy"; import type { GrowthCampaignType, GrowthPlatform } from "@/features/internal-growth/server/types";
export async function POST(req: Request) { try { await requireGrowthAgentOwnerContext(); const body = await req.json() as { featureId?: string; campaignType?: GrowthCampaignType; targetPlatforms?: GrowthPlatform[] }; if (!body.featureId || !body.campaignType) throw new ShopReelEndpointError("featureId and campaignType are required", 400); const out = await generateCampaign(body.featureId, body.campaignType, body.targetPlatforms ?? []); return NextResponse.json(out); } catch (error) { return toEndpointErrorResponse(error, "Failed to generate campaign drafts"); }}
