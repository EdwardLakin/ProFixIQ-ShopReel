export async function createOpportunityFromManualAsset(assetId: string) {
  return {
    ok: true,
    assetId,
    message: "Stub only. Connect this to your ShopReel opportunity pipeline next.",
  };
}
